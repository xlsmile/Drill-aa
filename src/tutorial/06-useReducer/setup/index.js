import React, { useState } from 'react';
import Modal from './Modal';
import { data } from '../../../data';

const Index = () => {
  return (
    <>
      <h2>Use Reducer</h2>
      <Modal />
    </>
  );
};

export default Index;
